# 项目展示声明

本 GitHub 仓库用于展示 LLM Agent 执行治理、tau2-bench 评测和 GRPO 后训练的工程实现。

当前已有真实证据包括：冻结 dev 集 H0/H1/H2 治理消融；RTX PRO 6000 上的 GPU
preflight、smoke 和正式 GRPO lineage；5 个可训练组、445 个 ART 梯度步；以及冻结 dev
选择的 checkpoint 0015。正式 lineage 在第 24 个位置触发预先声明的稀疏奖励终止规则。

最终 40 任务 × 8 trials × 4 cells 的 2×2 runner、双 alias serving 和报告生成器已经实现
并通过本地测试，但当前没有正式最终测试集四格结果，也没有已测得的模型效应、治理效应
或交互效应。

如后续为了展示报告格式加入预测数据，它只能作为示例，不属于真实实验结果；预测内容
应与正式 `results/` 隔离，并在出现处注明示例或预测状态。

真实结果以 `reports/governance_ablation.md`、`reports/grpo_training.md`、
`results/gpu/` 中的 manifests 和校验和为准。
